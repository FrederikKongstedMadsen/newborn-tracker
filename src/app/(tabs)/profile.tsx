import { Text } from 'react-native';

import { Screen } from '@/components/Screen';

export default function Profile() {
  return (
    <Screen topInset>
      <Text>Profile</Text>
    </Screen>
  );
}
