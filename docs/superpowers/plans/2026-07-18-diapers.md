# Diaper Tracker Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** One-tap diaper logging with editable history, Home card, Track entry — in the app's design language.

**Architecture:** Simplest tracker: no timers. Table + hooks mirror existing features; screens mirror the restyled sleep screens minus timer hero. Quick-log = instant insert mutations.

**Tech Stack:** Existing. No new dependencies.

## Global Constraints

- Specs: `docs/superpowers/specs/2026-07-18-diapers-design.md` (feature — includes the MANDATORY design-language section), `docs/superpowers/specs/2026-07-18-data-model-design.md` (table authoritative).
- Pattern sources (read before implementing): `src/app/sleep/index.tsx`, `src/app/sleep/[id].tsx`, `src/features/home/StatusCard.tsx`, `src/features/feeding/hooks.ts`.
- TS strict; created_by stamped on inserts; routes in protected block; dirty-field gating on edit; `mutedDark`-for-info contrast rule; `timeHHmm`/`relativeTime`/`localDateIso` from `@/lib/dates`.
- Verification per task: `npm run format && npm run typecheck && npm run lint && npm test` (+ `npx expo export --platform android && rm -rf dist` on navigation tasks). Migration application = controller/user checkpoint, never the implementer's.
- Commit bodies end: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`

---

### Task 1: Migration + types + diaperMath (TDD)

- Create `supabase/migrations/0005_diapers.sql` per spec (shared conventions incl. RLS, check constraint on type).
- Create `src/features/diaper/types.ts`: `DiaperType = 'pee' | 'poop' | 'both' | 'nothing'`; `Diaper { id, baby_id, happened_at, type: DiaperType, note, created_by, created_at }`; `NewDiaper = Pick<Diaper, 'baby_id' | 'happened_at' | 'type' | 'note'>`.
- Create `src/features/diaper/diaperMath.ts` + test: `todayCount(diapers: Diaper[], todayIsoStr: string): number` — count entries whose `localDateIso(happened_at)` equals todayIsoStr. TDD: today/yesterday boundary cases, empty list, expectations built via `localDateIso` (timezone-independent, mirror feedMath tests).
- Commit: `feat: add diapers schema, types, and today-count util`

### Task 2: Hooks

- Create `src/features/diaper/hooks.ts` mirroring feeding/hooks.ts structure, `['diapers']` key namespace: `useDiapers(babyId)` (desc by happened_at), `useLogDiaper()` (`NewDiaper`, created_by stamped), `useUpdateDiaper()` (`Partial<Pick<Diaper,'happened_at'|'type'|'note'>> & { id }`), `useDeleteDiaper()`. All invalidate `['diapers']` prefix. No hook tests (thin wrappers, repo convention).
- Commit: `feat: add diaper data hooks`

### Task 3: Screens

- Create `src/app/diaper/index.tsx`: Screen scroll={false} + FlatList. Header: quick-log Card — title-less grid/row of four type buttons (design language: PillButton neutral variant or tinted Pressables with diaper accent; label capitalized) each firing `useLogDiaper` with `happened_at = new Date().toISOString()`, all disabled while pending, error as danger text. Rows: diaper IconChip, bold capitalized type, muted `timeHHmm` + date, right column relativeTime + Avatar + display name (useProfileMap) — mirror SleepRow. Empty: "No diapers yet".
- Create `src/app/diaper/[id].tsx`: mirror sleep/[id] edit conventions — SegmentedControl `['Pee','Poop','Both','Nothing']` ↔ lowercase type mapping, datetime field dirty-gated, note, PillButton save (dirty-gated payload), danger delete behind Alert.
- Modify `src/app/_layout.tsx`: `diaper/index` ("Diaper") + `diaper/[id]` ("Edit diaper") in protected block.
- Commit: `feat: add diaper screens with one-tap logging`

### Task 4: Home card + Track row

- Create `src/features/home/DiaperStatusCard.tsx` via `StatusCard` (tracker 'diaper'): latest = diapers[0]; value = capitalized type; meta = `${relativeTime(happened_at, now)} · ${todayCount(diapers, todayIso())} today` (tick via `useNowTick(false)` for freshness); none → "No diapers yet — tap to log". onPress `/diaper`.
- Modify `src/app/(tabs)/index.tsx`: DiaperStatusCard after FeedingStatusCard (order per home.png: Sleep, Feeding, Diaper, Growth — reorder existing to match mock).
- Modify `src/app/(tabs)/track.tsx`: diaper row via trackerColors.
- Commit: `feat: add diaper home card and track entry`

## Self-Review Notes

- Covers spec DoD fully; lessons baked in: dirty gating, mutedDark rule, timeHHmm, tz-safe tests, running-row concerns N/A (no timer).
- Controller checkpoint: apply migration 0005 before live testing.
