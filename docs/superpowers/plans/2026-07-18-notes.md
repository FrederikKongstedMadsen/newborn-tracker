# Notes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Standalone notes feed per spec `docs/superpowers/specs/2026-07-18-notes-design.md`. Smallest feature — three tasks.

## Global Constraints

Same regime as diapers/sick plans: spec's design-language section MANDATORY; pattern sources diaper (or sleep) screens + StatusCard; TS strict; created_by stamping; dirty gating; mutedDark rule; dates helpers; per-task verification chain; migration = controller checkpoint; commit bodies end `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

---

### Task 1: Migration + types + hooks

- `supabase/migrations/0007_notes.sql` per spec.
- `src/features/notes/types.ts`: `Note { id, baby_id, noted_at, body, created_by, created_at }`, `NewNote = Pick<Note, 'baby_id' | 'noted_at' | 'body'>`.
- `src/features/notes/hooks.ts`: `useNotes(babyId)` (desc noted_at), `useAddNote()`, `useUpdateNote()` (`Partial<Pick<Note,'noted_at'|'body'>> & { id }`), `useDeleteNote()` — `['notes']` namespace, standard idioms.
- Commit: `feat: add notes schema, types, and hooks`

### Task 2: Screens

- `src/app/notes/index.tsx`: Screen scroll={false} + FlatList; header composer Card (multiline body FormField ~3 lines, datetime `YYYY-MM-DDTHH:mm` default now, "Save note" PillButton — disabled on empty/whitespace body or invalid datetime, pending-disable, danger error, success clears body+resets datetime); rows: notes IconChip, body `numberOfLines={2}` in `colors.text`, muted datetime line, right relativeTime + Avatar + name; empty state "No notes yet". Tap → `/notes/[id]`.
- `src/app/notes/[id].tsx`: full multiline body + datetime, both dirty-gated; Alert-confirmed delete; standard conventions.
- Register `notes/index` ("Notes") + `notes/[id]` ("Edit note") in protected block.
- Commit: `feat: add notes screens`

### Task 3: Home card + Track row

- `src/features/home/NotesStatusCard.tsx` via StatusCard (tracker 'notes'): latest body single-line as value, relativeTime meta (useNowTick(false)), empty prompt; onPress `/notes`.
- Home grid order finalizes to mock: Sleep, Feeding, Diaper, Growth, Sick, Notes. Track row "Notes".
- Commit: `feat: add notes home card and track entry`

## Self-Review Notes

- DoD covered; no new pure logic so no new unit tests (suite stays green).
- After this feature, all six mock home cards exist — grid matches home.png fully.
