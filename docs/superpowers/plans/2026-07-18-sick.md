# Sick Tracker Implementation Plan (Temperature + Medicine)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Temperature + paracetamol dose logging with last-dose banner, per spec `docs/superpowers/specs/2026-07-18-sick-design.md`.

**Architecture:** Two small tables, one screen with a segmented split, two edit routes. No timers beyond a coarse relative-time tick. Mirrors diaper feature structure (build diapers first — this plan assumes its conventions exist as pattern sources; if not yet built, use sleep screens as patterns instead).

## Global Constraints

Same as diapers plan: design-language section of the spec is MANDATORY; pattern sources sleep/diaper screens + StatusCard; TS strict; created_by stamping; dirty gating; mutedDark rule; dates helpers; per-task verification chain; migration = controller checkpoint; commit bodies end `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

---

### Task 1: Migration + types + sickMath (TDD)

- `supabase/migrations/0006_sick.sql` per spec (both tables, RLS, checks).
- `src/features/sick/types.ts`: `Temperature { id, baby_id, measured_at, celsius, note, created_by, created_at }`, `NewTemperature`; `MedicineDose { id, baby_id, given_at, medicine, amount, unit: 'ml' | 'mg', note, created_by, created_at }`, `NewMedicineDose` (Pick patterns as elsewhere).
- `src/features/sick/sickMath.ts` + test (TDD): `isFever(celsius: number): boolean` (≥ 38.0; pin 37.9 false / 38.0 true); `doseSummary(dose): string` → `"2.5 ml paracetamol"` (trim trailing `.0`: `"2 ml"` not `"2.0 ml"` — pin both cases).
- Commit: `feat: add sick schema, types, and utils`

### Task 2: Hooks

- `src/features/sick/hooks.ts`: `useTemperatures(babyId)` (desc measured_at), `useLogTemperature()`, `useUpdateTemperature()`, `useDeleteTemperature()`; `useDoses(babyId)` (desc given_at), `useLogDose()`, `useUpdateDose()`, `useDeleteDose()`. Key namespaces `['temperatures']` / `['medicine_doses']`, each mutation invalidates its own prefix. Mirror feeding/hooks.ts idioms.
- Commit: `feat: add sick data hooks`

### Task 3: Sick screen (segmented)

- `src/app/sick/index.tsx`: Screen scroll={false}; header under title = `SegmentedControl ['Temperature', 'Medicine']`.
  - Temperature segment: inline form Card (celsius FormField decimal-pad w/ comma→dot parse — reuse/replicate `parseDecimal` conventions; datetime `YYYY-MM-DDTHH:mm` default now; note; validation: parses + 30 ≤ c ≤ 43 sanity), submit PillButton → `useLogTemperature`, success clears; FlatList of temps: temperature IconChip, bold `${celsius.toFixed(1)} °C` styled `colors.danger` when `isFever`, muted datetime, Avatar + relativeTime.
  - Medicine segment: last-dose banner Card ("Last paracetamol · 3h 20m ago" via relativeTime + `useNowTick(false)`; none → "No doses logged"); inline dose form (amount decimal, unit SegmentedControl ml|mg, medicine FormField default 'paracetamol', datetime, note; validation amount > 0), submit → `useLogDose`; dose FlatList rows (`doseSummary` bold, datetime, Avatar).
- Routes: register `sick/index` ("Sick") in protected block.
- Commit: `feat: add sick screen with temperature and medicine segments`

### Task 4: Edit routes

- `src/app/sick/temperature/[id].tsx` + `src/app/sick/medicine/[id].tsx`: mirror diaper/[id] conventions — find row by param from the list hook, render-time prefill sync, dirty-gated saves, Alert-confirmed deletes, PillButtons. Register both in protected block ("Edit temperature" / "Edit dose").
- Commit: `feat: add sick edit screens`

### Task 5: Home card + Track row

- `src/features/home/SickStatusCard.tsx` via StatusCard (tracker 'temperature'): value = latest temp formatted (danger color when fever — StatusCard may need optional `valueColor` prop; add additively); meta = latest dose `"paracetamol · 3h ago"` if any else temp relativeTime; none → "No entries yet". onPress `/sick`.
- Home order per mock: Sleep, Feeding, Diaper, Growth, Sick(temperature), Notes-future. Track row "Sick".
- Commit: `feat: add sick home card and track entry`

## Self-Review Notes

- DoD covered. StatusCard valueColor = only primitive touch (additive prop).
- Fever threshold hardcoded 38.0 per spec out-of-scope note.
